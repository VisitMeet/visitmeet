<script src="http://maps.googleapis.com/maps/api/js?libraries=places&sensor=false"></script>
<script src="jquery.geocomplete.js"></script>
<%= simple_form_for @product do |p| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@product.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @produt.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= p.input :title %>
  </p>
  <p>
    <%= p.input :description %>
  </p>
  <p>
    <%= p.input :price %>
  </p>
  <p>
    <%= p.input :latitude %>
  </p>
  <p>
    <%= p.input :longitude %>
  </p>
  <p>
    <%= p.input :location %>
  </p>
  <p>
      <%= p.select :category, options_for_select(Product.categories.map {|k, v| [k.humanize.capitalize, k]}) %>
  </p>
  <%= p.button :submit, class: "btn btn-primary" %>
<% end %>
<script type="text/javascript">
  $("#product_location").geocomplete();

</script>
